# ToDoList
internship todolist


the front end is client and the backend is server
install dependencies as usual on both the front end and the backend
run the backend with npm start
and run the frontend with npm run dev
